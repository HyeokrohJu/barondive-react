import { container } from '~/assets/jss/material-kit-react';

const mainStyle = () => ({
  container,
  main: {
    position: 'relative',
    zIndex: '3',
    minHeight: '500px',
  },
  mainRaised: {
  },
});

export default mainStyle;
